import styled from 'styled-components';

export const SectionStyled = styled.section`
  text-align: center;
  font-size: ${({ theme }) => theme.fontSizes.m};
  fontweights: ${({ theme }) => theme.fontWeights.bold};
`;
